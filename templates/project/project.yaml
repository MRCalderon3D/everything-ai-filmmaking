# production/project.yaml — workspace configuration
# Schema: schemas/project.schema.json
# Every command inherits these defaults; override per run with flags.

title: "The Red Suitcase"          # working title of the production
production_type: cinema            # cinema | commercial — selects rules/cinema/ or rules/commercial/
format: short                      # cinema: short | feature | series · commercial: spot | social | branded-content | music-video
logline: "A woman fleeing her past finds a stranger's red suitcase on a rain-soaked platform — and inside it, proof that her past is fleeing her."
genre: noir                        # primary genre label

aspect_ratio: "16:9"               # width:height — quote it so YAML keeps it a string
fps: 24                            # target frames per second
resolution: "1920x1080"            # delivery resolution, WIDTHxHEIGHT

deliverables:                      # delivery variants — REQUIRED for commercial projects
  - platform: festival             # theatrical | festival | streaming | tv | web | youtube | youtube-shorts | instagram | tiktok | x | other
    aspect_ratio: "16:9"
    resolution: "1920x1080"
    notes: "Single master deliverable; cinema projects usually list exactly one."
# For a commercial project, list every platform variant and design them at
# blocking time — a 9:16 cut is a designed reframe, never a lazy crop:
#  - platform: instagram
#    aspect_ratio: "9:16"
#    duration_seconds_max: 30
#    notes: "Safe area for UI overlays; must read with sound off."

lens_kit:                          # the project's finite lens discipline — shots must stay inside it
  focal_lengths_mm: [28, 50, 85]   # allowed focals (full-frame equivalent); single-lens projects are legitimate
  lens_type: spherical             # spherical | anamorphic | macro | tilt_shift | fisheye
  # anamorphic_squeeze: 2          # required when lens_type is anamorphic (1.33 | 1.5 | 1.65 | 1.8 | 2)
  character: "vintage warmth, gentle halation, soft highlight roll-off"
  notes: "28 for the station's geography, 50 for Mara neutral, 85 for the suitcase discoveries."

style:                             # global visual anchors; detail lives in the style bible
  look_refs:                       # approved LOOK_* style reference assets
    - LOOK_NOIR_NIGHT_V01
  influences:                      # films / photographers / movements that anchor the look
    - "The Third Man (1949) — wet cobblestone night exteriors"
    - "Saul Leiter — color reflections through glass"
  notes: "Sodium-vapor warmth against blue-hour shadow; rain on every exterior."

default_providers:                 # default generation backend per kind
  image: fal                       # fal | replicate | comfyui | harness-native (no API key — e.g. gpt-image on Codex)
  video: veo                       # veo | kling | runway | seedance | fal | manual (hand generation in a web UI, e.g. Higgsfield)
  audio: elevenlabs                # elevenlabs | fal | local

created_at: "2026-07-13T00:00:00Z" # set by create-project.js
version: "0.1.0"                   # workspace metadata version
